<h1>Categories</h1>
  <% @groups.each do |group| %>
    <%= group.name %>
    <image src="<%= (url_for(group.icon)) %>" class="icon" >
    <%= group.payments.sum(:amount) %>
  <% end %>

<%= link_to 'Add Category', new_user_group_path%>