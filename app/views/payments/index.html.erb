<h1>Transactions</h1>
  <%= @sum %>

<% @payments.each do |payment| %>
  <div>
    <%= payment.name %>
    <%= payment.amount%>
  </div>
<% end %>

<%= link_to 'Add Payment', new_user_group_payment_path%>
